import 'package:flutter/material.dart';
import 'app.dart';

/// The entry point of the KTX application.
///
/// This file contains the main function which initializes and runs
/// the [MyApp] widget. Keeping the main function clean allows
/// developers to focus on the application logic inside [MyApp].
void main() {
  runApp(MyApp());
}